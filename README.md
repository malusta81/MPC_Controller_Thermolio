# MPC_Controller_Thermolio
This repository contains a Python implementation of Model Predictive Control (MPC) divided into two components: Edge and Cloud. The Edge collects real-time sensory data and executes control commands, while the Cloud processes the MPC algorithm and generates those commands.
